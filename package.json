{
  "name": "adb-proxy-daemon",
  "version": "0.1.0",
  "description": "ADBPD — Sovereign multi-device ADB host replacement (Future ATI LLC)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "start": "bun run src/main.ts",
    "test": "bun test",
    "test:unit": "bun test tests/unit",
    "test:integration": "bun test tests/integration",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\""
  },
  "dependencies": {
    "@yume-chan/adb": "2.6.0",
    "@yume-chan/adb-server-node-tcp": "2.5.2",
    "hono": "^4.0.0",
    "pino": "^9.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.0"
  },
  "trustedDependencies": []
}
